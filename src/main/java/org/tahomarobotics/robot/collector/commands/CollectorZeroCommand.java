package org.tahomarobotics.robot.collector.commands;

import edu.wpi.first.wpilibj2.command.Command;

//i'm not even sure if this is necessary because the collector should start at zero whenever the robot is turned on
//like just flip it up like last year
//or if a zero command is even possible because we don't have an absolute encoder
public class CollectorZeroCommand extends Command {
}
